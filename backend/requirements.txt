# UMBRA local ML backend — core service dependencies.
#
# This file installs only what the *service* needs to run, list providers and
# serve the timeline. Heavy trained-model dependencies are deliberately split
# into requirements-ace-step.txt / requirements-extras.txt so a developer can
# bring up the app, see the honest "not installed" states, and add models later.

fastapi>=0.110.0
uvicorn[standard]>=0.27.0
python-multipart>=0.0.18
httpx>=0.27.0

# Audio decoding and the real-result contract.
soundfile>=0.13.1
numpy>=1.24

# Model download from official distribution channels.
huggingface-hub>=0.24.0

# Integration credential vault: AES-256-GCM encryption at rest for the
# Freesound (and future integration) credentials database. The key itself
# comes from the server-only UMBRA_CREDENTIAL_ENCRYPTION_KEY env var.
cryptography>=42.0.0

# Optional at runtime (guarded import): loads a local .env for the backend.
python-dotenv>=1.0.0

# Optional but recommended: ffprobe-based video metadata in analysis/video.py
# needs the `ffmpeg` binary on PATH, not a Python package.

# Tests
pytest>=8.0
