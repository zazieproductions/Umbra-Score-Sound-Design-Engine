# Umbra Score ML Backend Requirements

# Core (always needed)
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
pydantic>=2.5.0
python-multipart>=0.0.6

# ML Framework
torch>=2.0.0
torchaudio>=2.0.0

# Audio Processing
soundfile>=0.12.1
numpy>=1.24.0

# Transformers & Diffusers (for Stable Audio, MMAudio)
transformers>=4.35.0
diffusers>=0.25.0
accelerate>=0.25.0

# CLAP
# laion-clap  # Install separately: pip install laion-clap

# Scene Detection
# scenedetect[videoio]>=0.6.0  # Install separately
# opencv-python>=4.8.0  # Install separately

# Video Processing (requires ffmpeg installed separately)
# ffmpeg-python>=0.2.0  # Optional

# Optional: Progress bars
tqdm>=4.66.0
